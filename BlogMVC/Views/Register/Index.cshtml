@model EntityLayer.Concrete.Writer


@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/UserLayout.cshtml";
}

<script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
<script src="~/lib/jquery/dist/jquery.js"></script>

<section class="main-content-w3layouts-agileits">
    <div class="container">
        <h3 class="tittle">Yazar Kayıt Sayfası</h3>
        <div class="inner-sec">
            <div class="login p-5 bg-light mx-auto mw-100">
                @* @using(Html.BeginForm("Index","Register",FormMethod.Post))*@
                <form asp-action="Index" asp-controller="Register" method="post">
                    
                    <div class="form-row">
                        <div class="col-md-6 mb-3">
                            <label for="validationCustom01">Adınız Soyadınız</label>
                          @*  @Html.TextBoxFor(w=>w.WriterName)
                            @Html.ValidationMessageFor(w=>w.WriterName)*@
                             <input type="text" class="form-control" id="validationDefault01" name="WriterName" placeholder="">
                                <span asp-validation-for="WriterName" class="text-danger"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="validationCustom02">Mail Adresi</label>
                           @* @Html.TextBoxFor(w=>w.WriterMail)
                            @Html.ValidationMessageFor(w=>w.WriterMail)*@
                            <input type="email" class="form-control" name="WriterMail" id="validationDefault02" placeholder="">
                                <span asp-validation-for="WriterMail" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group col-md-6">
                            <label for="exampleInputPassword1 mb-2">Şifre</label>
                         @*   @Html.TextBoxFor(w=>w.WriterPassword)
                            @Html.ValidationMessageFor(w=>w.WriterPassword)*@
                             <input type="password" class="form-control" name="WriterPassword" id="password1" placeholder="">
                                <span asp-validation-for="WriterPassword" class="text-danger"></span>
                        </div>
                        <div class="form-group col-md-6">
                            <label for="exampleInputPassword2 mb-2">Şifreyi Onayla</label>
                           @* @Html.TextBoxFor(w=>w.WriterPasswordConfirm)*@
                             <input type="password" class="form-control" name="WriterPasswordConfirm" id="password2" placeholder="">
                        </div>
                        <div class="form-group col-md-6">
                            <label for="exampleInputPassword2 mb-2">Görsel Dosya Yolu</label>
                           @* @Html.TextBoxFor(w=>w.WriterImage)
                            @Html.ValidationMessageFor(w=>w.WriterImage)*@
                             <input type="text" class="form-control" name="WriterImage" id="password2" placeholder="">
                        </div>
                        <div class="form-group col-md-6">
                            <label for="validationCustom01 mb-2">Şehir</label>
                            @*         @Html.DropDownList("Cities",
                                //(IEnumerable<SelectListItem>)ViewBag.Cities,
                                //"Select country")*@
                            @*@Html.DropDownListFor(x => x.WriterCity, (List<SelectListItem>)ViewBag.Cities, new { @class = "form-control dropdown" })*@
                            @* @Html.TextBoxFor(w=>w.WriterCity)
                                @Html.ValidationMessageFor(w=>w.WriterCity)*@
                                <select asp-items="@ViewBag.Cities" class="form-control">
                                    <option selected disabled>Seçiniz</option>
                                </select>
                                <span asp-validation-for="WriterCity" class="text-danger"></span>
                        </div>

                    </div>
                    <button type="submit" class="btn btn-primary submit mb-4">Kayıt Ol</button>
                    <p>
                        <a href="#" id="btn1">Tüm şartları okudum, kabul ediyorum.</a>
                    </p>
                </form>
                @*}*@

            </div>
        </div>
    </div>
    <br />
    <script>
        $('#btn1').click(function(){
            swal('Kullanım Şartları','Tüm şartları kabul etmiş oldunuz. İyi eğlenceler dileriz!');
        });
    </script>
</section>

